import { getTodos } from '../stateReducer'

describe('#todos state', () => {
  describe('#getTodos', () => {
    it('should get some todos off state', () => {
      // const state = {...}
      // expect(getTodos(state)).toEqual()
    })
  })
})
